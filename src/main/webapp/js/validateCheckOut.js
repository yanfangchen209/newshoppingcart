/**
 * after dom fully loaded, execute js code below:
 using dom methods to get all input elements->add event handler to these input elements->validate each of the element: zipcode and phone using regx,
 other not empty or null if(!firstNameInput.value)(set erroe message and return false)else{return true;}->add event listener(submit) to form, if any 
 of the fields invalid, event.preventDefault, if all valid, return true;
 */